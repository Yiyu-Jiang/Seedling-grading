读取 /ldap_shared/home/t_jyy/ssd/home/jiangyiyu/unit3code/mydata/org_data 
中的原始图像和 JSON 标注，
生成原图和缩放图上的标注 overlay。必须保证：只要图像尺寸发生变化，就同步更新 JSON 中的 imageWidth 和 imageHeight；
同时严格检查每一个 JSON 标注是否准确显示在图像对应目标上。
不要为了匹配而胡乱生成新标注、增删标注点或随意修改局部轮廓，只允许对同一张图中的所有标注统一施加全局几何变换，
如缩放、平移、旋转、翻转或统一仿射变换。系统测试多种候选变换，并通过将标注 mask 与原图自动提取的绿色植株前景进行比较，
使用 IoU、Dice、前景覆盖率、背景误检率和实例中心距离等指标综合评分，自动选择最优方案。
只有在原图阶段确认每个实例都基本准确匹配后，才能继续将图像缩小为原来的 1/2，并同步缩放标注坐标，
同时更新缩放后 JSON 的 imageWidth 和 imageHeight。
输出原图 overlay、缩放图 overlay、修正后的 JSON、前景提取图和日志，并标明每张图采用的最终变换方案及实例级匹配结果。
如果不能通过统一全局变换实现可靠匹配，则明确标记为失败，而不是胡乱改标注点。